import Link from "next/link";
import { getDashboardCounts } from "@/lib/data";
export const dynamic="force-dynamic";
export default async function Dashboard(){const c=await getDashboardCounts();const cards=[["Projects",c.projects],["Published projects",c.publishedProjects],["Draft projects",c.draftProjects],["Articles",c.blogs],["Published articles",c.publishedBlogs],["Draft articles",c.draftBlogs],["Messages",c.contacts],["Unread messages",c.unread]];return <><div className="dash-heading"><div><p className="eyebrow">Content management</p><h1>Overview</h1></div><div><Link className="button secondary" href="/dashboard/projects/new">New project</Link> <Link className="button" href="/dashboard/blogs/new">New article</Link></div></div><div className="stats-grid">{cards.map(([label,value])=><div className="stat-card" key={label}><span>{label}</span><strong>{value}</strong></div>)}</div></>}
